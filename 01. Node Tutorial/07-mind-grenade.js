const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}

addValues();
// if we have a function that is invoked inside the module,
// the function code will also run when we import the file/module!
